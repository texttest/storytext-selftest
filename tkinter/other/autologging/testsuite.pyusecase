# Produce a logical description if the buttons are positioned elsewhere
buttons_at_bottom

# Check we show what is actually shown in an entry
password_entry

# Make sure we can handle popup menus also
popup_menu

# If text appears in a subwindow, make sure we aren't monitoring it for updates after the subwindow has been closed
text_in_subwindow
