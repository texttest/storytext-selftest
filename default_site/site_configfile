# Fallback site-specific config file, if there isn't one at site/site_configfile
# We point the checkout as if a tarball has just been downloaded alongside the tests

checkout_location:${TEXTTEST_HOME}/../PyUseCase
