[Label=Select All]
Selection = select all

[Label=Exit]
Selection = exit

[Type=Shell]
Resize = resize window to
Close = close window

[Editor=org.storytext.rcp.gef.basic.graph1, Viewer]
Select = select
