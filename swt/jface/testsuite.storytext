# Test for decorations showing whether a field is valid
text_decoration

# Simulates typing instead for setting text in text fields
typed_text

# Simulates typing instead for setting text in text fields
typed_text_combo

# TableCursor enables individual cell selection, try to workaround SWTBot's habit of
# selecting the entire row anyway
cell_selection

# Make sure we notice autocomplete fields
autocomplete

# Show custom tooltip info, and allow it to be disabled
custom_tooltips
