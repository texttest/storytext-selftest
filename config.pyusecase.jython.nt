# Sadly Jython has no native Windows launcher, so we have to resort to bat files

interpreter:

executable:${TEXTTEST_CHECKOUT}/${PLATFORM_DIR}/bin/pyusecase.bat

[run_dependent_text]
output:Ctrl\+Shift{REPLACE Shift+Ctrl}
